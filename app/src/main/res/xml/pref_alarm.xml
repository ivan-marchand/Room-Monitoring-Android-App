<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory
        android:title="@string/pref_header_alarm"
        android:key="pref_key_alarm_settings">

        <!-- A 'parent' preference, which enables/disables child preferences (below)
             when checked/unchecked. -->
        <CheckBoxPreference
            android:key="temperature_alarm"
            android:title="@string/pref_title_temperature_alarm"
            android:defaultValue="false" />

        <!-- Allows the user to choose a ringtone in the 'notification' category. -->
        <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
        <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
        <RingtonePreference
            android:dependency="temperature_alarm"
            android:key="alarm_ringtone"
            android:title="@string/pref_title_ringtone"
            android:ringtoneType="alarm"
            android:defaultValue="content://settings/system/alarm_silent" />

        <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
        <CheckBoxPreference
            android:dependency="temperature_alarm"
            android:key="alarm_vibrate"
            android:title="@string/pref_title_vibrate"
            android:defaultValue="true" />

        <EditTextPreference
            android:dependency="temperature_alarm"
            android:key="alarm_max_value"
            android:title="@string/pref_title_max_value"
            android:defaultValue="75"
            android:inputType="numberSigned" />

        <EditTextPreference
            android:dependency="temperature_alarm"
            android:key="alarm_min_value"
            android:title="@string/pref_title_min_value"
            android:defaultValue="70"
            android:inputType="numberSigned" />


    </PreferenceCategory>
</PreferenceScreen>
